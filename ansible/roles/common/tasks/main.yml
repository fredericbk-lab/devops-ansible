- name: Installer paquets de base
  apt:
    name:
      - git
      - curl
      - ca-certificates
      - python3
      - python3-pip
      - python3-venv
    state: present
    update_cache: yes

- name: Créer le dossier racine apps
  file:
    path: "{{ apps_root }}"
    state: directory
    mode: "0755"
